// This mix the text: string and number: number
function combinedTextAndNumber(text, number) {
    // Then combined the text: string and number: number    
    return text + number;
}
;
// Now trying with "Age:" and 30
console.log(combinedTextAndNumber("Age:", 30));
// Output will be Age:30
