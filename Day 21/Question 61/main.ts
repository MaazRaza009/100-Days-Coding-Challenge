// First, we make the list of (enum).
enum vehicleCategories{
    Car,
    Truck,
    Motorcycle
};
// Then, console them to check the one type of vehicle.
// Enum start the list with "0" thats why they showing "2".
console.log(vehicleCategories.Motorcycle);
