//First to form simple list
var numbersList = [1, 2, 3, 4, 5, 6];
//Now double each number
var doubleNumbers = numbersList.map(function (numberList) { return numberList * 2; });
//Then console the doubleNumbers
console.log(doubleNumbers);
